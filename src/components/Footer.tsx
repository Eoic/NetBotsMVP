import React from 'react';

const Footer = () => {
  return (
    <footer className='footer'>
        &copy; {new Date().getFullYear()} NetBots
    </footer>
  )
};

export { Footer };
export default Footer;
